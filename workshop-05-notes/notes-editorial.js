/* Presentation only. Authentication and approved article text are untouched. */
(()=>{const progress=document.querySelector('.reading-progress');let pending=false;const update=()=>{const range=document.documentElement.scrollHeight-innerHeight;progress.style.width=(range>0?Math.min(100,Math.max(0,scrollY/range*100)):0)+'%';pending=false};addEventListener('scroll',()=>{if(!pending){pending=true;requestAnimationFrame(update)}},{passive:true});addEventListener('resize',update);update();document.querySelector('[data-open-toc]')?.addEventListener('click',()=>{document.querySelector('#contents').open=true})})();
